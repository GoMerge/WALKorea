name: Deploy to EC2

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. 레포지토리 checkout
      - uses: actions/checkout@v4

      # 2. 배포용 폴더 준비
      - name: Prepare deploy folder
        run: |
          rm -rf deploy
          mkdir deploy
          cp -r app docker-compose.yml deploy/

      # 3. SCP로 EC2에 전송
      - name: Copy to EC2
        uses: appleboy/scp-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          source: "app,data,db,frontend,Dockerfile,docker-compose.yml,requirements.txt"
          target: "~/WALKorea"
          overwrite: true

      # 4. SSH로 EC2 접속 → Docker Compose 실행
      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            cd ~/WALKorea
            # 환경변수 설정
            export MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}
            export MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}
            export MYSQL_USER=${{ secrets.MYSQL_USER }}
            export MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}
            export HOST_PORT=${{ secrets.HOST_PORT }}
            export DB_PORT=${{ secrets.DB_PORT }}
            export REDIS_HOST=${{ secrets.REDIS_HOST }}
            export REDIS_PORT=${{ secrets.REDIS_PORT }}
            export REDIS_DB=${{ secrets.REDIS_DB }}   

            # 강제 정리 
            docker-compose down --remove-orphans --volumes --timeout 30
            docker system prune -f --volumes
            docker network prune -f

            # 재생성 
            docker-compose up -d --build --force-recreate

            # 배포 완료 확인
            sleep 5
            docker ps
